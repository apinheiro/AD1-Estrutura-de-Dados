\begin{algorithm}
    \caption{Inverte_Lista(L${}_1$,n)}
    \Entrada{Uma lista encadeada L${}_1$ com n > 0 elementos e nó cabeça PT${}_1$}
    \Saida{Lista encadeada L${}_2$ com os elementos invertidos de L sem repetição e nó cabeça PT${}_2$}
    L${}_2$ $\leftarrow$ ocupar(PT${}_2$);
    pt$_1$ $\leftarrow$ PT${}_1$;
    pt$_2$ $\leftarrow$ PT${}_2$;    
    \Enquanto{pt$_1$ $\neq \lambda$}{
       

    }
\end{algorithm}